syntax = "proto3";

package fraud_detection;

service fraud_detection {
    rpc fraud_detect (f_detectionRequest) returns (f_detectionResponse);
}

message f_detectionRequest {
    string name = 1;
    int32 id =2 ;
}

message f_detectionResponse {
    bool fraudResult = 1;
}
